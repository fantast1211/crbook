eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:8000/eureka
server:
  port: 8888

spring:
  application:
    name: crbook-config  # 应用名称
  cloud:
    config:
      server:
        git:
          uri: https://github.com/fantast1211/crbook/tree #配置文件所在仓库
          username: fantast1211
          password: jm456852456
          default-label: master #配置文件分支
          search-paths: config  #配置文件所在根目录